
<% include partials/header %>

<body id="kt_body" class="bg-body">
  <%- include('partials/message') %>

  <!--begin::Main-->
  <!--begin::Root-->
  <div class="d-flex flex-column flex-root">
    <!--begin::Authentication - Sign-up -->
    <div
      class="d-flex flex-column flex-column-fluid bgi-position-y-bottom position-x-center bgi-no-repeat bgi-size-contain bgi-attachment-fixed"
      style="background-image: url(/media/illustrations/sketchy-1/14.png"
    >
      <!--begin::Content-->
      <div
        class="d-flex flex-center flex-column flex-column-fluid p-10 pb-lg-20"
      >
        <!--begin::Logo-->
        <!--end::Logo-->
        <!--begin::Wrapper-->
        <div class="w-lg-600px bg-body rounded shadow-sm p-10 p-lg-15 mx-auto">
          <!--begin::Form-->
          <form action="/updateBank" method="post" class="form w-100" novalidate="novalidate" id="kt_sign_up_form">
            <!--begin::Heading-->
            <div class="mb-10 text-center">
              <!--begin::Title-->
              <h1 class="text-dark mb-3">Modifica un Banco</h1>
              <!--end::Title-->
              <!--begin::Link-->
              <!--end::Link-->
            </div>
            <!--begin::Input group-->
            <div class="fv-row mb-5">
              <label class="form-label fw-bolder text-dark fs-6"
                >Seleccione el Banco</label
              >
              <select name="bankId" id="bankId" class="form-select form-select-solid">
                  <% if(banks){  %>
                    <% for(var i=0; i < banks.length; i++) { %>
                      <option value="<%= banks[i].id %>"><%= banks[i].name %></option>
                      <% } %>
                  <% } %>
              </select>
              
            </div>
            <div class="fv-row mb-7">
              <label class="form-label fw-bolder text-dark fs-6">Escriba el Nombre del Banco</label>
              <input
                class="form-control form-control-lg form-control-solid"
                type="text"
                placeholder=""
                name="name"
                autocomplete="off"
              />
            </div>
            <div class="fv-row mb-5">
              <label class="form-label fw-bolder text-dark fs-6"
                >Esta el servicio de consulta de saldo activo?</label
              >
              <select name="isBalanceServiceActive" id="isBalanceServiceActive" class="form-select form-select-solid">
                <option value="true">Si</option>
                <option value="false">No</option>
              </select>
              
            </div>
            <div class="fv-row mb-5">
              <label class="form-label fw-bolder text-dark fs-6"
                >Esta el servicio de pagos activos?</label
              >
              <select name="isPaymentServiceActive" id="isPaymentServiceActive" class="form-select form-select-solid">
                <option value="true">Si</option>
                <option value="false">No</option>
              </select>
              
            </div>
            <div class="text-center">
              <button
                type="submit"
                class="btn btn-lg btn-primary"
              >
                <span class="indicator-label">Submit</span>
                <span class="indicator-progress"
                  >Please wait...
                  <span
                    class="spinner-border spinner-border-sm align-middle ms-2"
                  ></span
                ></span>
              </button>
            </div>
            <!--end::Actions-->
          </form>
          <!--end::Form-->
        </div>
        <!--end::Wrapper-->
      </div>
      <!--end::Content-->
      <!--begin::Footer-->
      <% include partials/footer %>
<!-- Footer -->

